apiVersion: v1
kind: Service

metadata:
  name: anator-postgres-service

spec:
  selector:
    app: anator-postgres

  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

  type: ClusterIP
