apiVersion: v1
kind: Pod

metadata:
  name: backend-pod

spec:
  containers:
    - name: backend
      image: kubernetes-anator-app-backend:latest
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 3000